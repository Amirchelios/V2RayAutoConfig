name: 🔗 TrustLink Auto Update (DEPRECATED - Now integrated in main workflow)

on:
  # این workflow غیرفعال شده چون حالا در workflow اصلی V2Ray AutoConfig ادغام شده
  workflow_dispatch:  # فقط برای اجرای دستی (برای تست)
  # schedule:
  #   # هر ساعت
  #   - cron: "0 * * * *"
  #   # هر نیم ساعت (برای تست)
  #   - cron: "*/30 * * * *"
  # push:
  #   paths:
  #     - 'Files/trustlink.py'
  #     - '.github/workflows/trustlink.yml'

concurrency:
  group: trustlink-update
  cancel-in-progress: true

jobs:
  update-trustlink:
    runs-on: ubuntu-latest
    name: "به‌روزرسانی خودکار TrustLink (غیرفعال)"
    
    steps:
      - name: ℹ️ Workflow Deprecated
        run: |
          echo "⚠️ این workflow غیرفعال شده است!"
          echo "🔗 TrustLink حالا در workflow اصلی V2Ray AutoConfig ادغام شده"
          echo "📅 اجرا: هر 2 ساعت بعد از اتمام V2Ray AutoConfig"
          echo "✅ نیازی به اجرای جداگانه نیست"
          exit 0
