name: 🔗 TrustLink Auto Update (REMOVED - No longer needed)

on:
  # این workflow غیرفعال شده چون حالا در workflow اصلی V2Ray AutoConfig ادغام شده
  workflow_dispatch:  # فقط برای اجرای دستی (برای تست)
  # schedule:
  #   # هر ساعت
  #   - cron: "0 * * * *"
  #   # هر نیم ساعت (برای تست)
  #   - cron: "*/30 * * * *"
  # push:
  #   paths:
  #     - 'Files/trustlink.py'
  #     - '.github/workflows/trustlink.yml'

concurrency:
  group: trustlink-update
  cancel-in-progress: true

jobs:
  update-trustlink:
    runs-on: ubuntu-latest
    name: "به‌روزرسانی خودکار TrustLink (غیرفعال)"
    
    steps:
      - name: ℹ️ Workflow Removed
        run: |
          echo "❌ این workflow حذف شده است!"
          echo "🔗 TrustLink Updater از منو workflow حذف شد"
          echo "📅 TrustLink حالا به صورت هوشمند بعد از V2Ray AutoConfig اجرا می‌شود"
          echo "✅ نیازی به اجرای جداگانه نیست"
          exit 0
